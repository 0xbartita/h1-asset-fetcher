"""Textual TUI — a single-screen, progressively-revealed dashboard that drives
the fetch → browse → download pipeline. Launched by `h1-asset-fetcher` with no
arguments, or `python3 -m h1_asset_fetcher.tui`."""
