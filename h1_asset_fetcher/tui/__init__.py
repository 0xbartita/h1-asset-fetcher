"""Interactive terminal wizard — clean inline prompts (questionary) that ask
through platform → credentials → scope → fetch → download. Launched by
`h1-asset-fetcher` with no arguments, or `python3 -m h1_asset_fetcher.tui`."""
